# Guild Schema Manager

Discord サーバー構成を YAML スキーマとして export / diff / apply する Bot 実装です。

## Quick Start

```bash
uv sync
uv run python -m bot
```

## Run Tests

```bash
uv run pytest
```

## フォーマットとLint

```bash
uv run ruff format
uv run ruff check
```
